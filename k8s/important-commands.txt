# Secrets Creation - Secrets valid till Cluster is valid
kubectl create secret generic app-secrets --from-env-file=.env  
kubectl create secret generic aws-credentials --from-env-file=aws.env  

# Port Forwarding - Local Testing
kubectl port-forward pod/<pod-name> 3001:8080